using Scanner.Domain.Facebook;

namespace Scanner.Infrastructure
{
    public class FacebookService : IFacebookService
    {
        public GetPostsResult GetPosts(FacebookHandle handle)
        {
            // var client = new FacebookClient();
            // client.Login("", "");
            // var getPostsResult = client.GetPosts(handle.Value);
            // translate getpostsresult
            return null;
        }
    }
}
